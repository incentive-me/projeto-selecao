export type PaymentValues = {
  nome: string;
  descricao: string;
  valor_inicial: number;
  saldo_id: string;
  id: number;
  usuario_id: number;
};

export type PaymentData = {
  nome: string;
  descricao: string;
  valor: number;
};
